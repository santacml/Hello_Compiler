program test_program is
    integer result;
	procedure test(integer i in)
        integer result;
        integer asdf;
		begin
            /*
            //if(true) then i := 5.0; should this error?
            if(true) then i := 5;
            else i := 0;
            end if;
            
            */
			for(asdf := 0; asdf < 5)
				result := i;
			end for;
            
		end procedure;
begin
    test(result);
    result := 3 + 4;
end program.
