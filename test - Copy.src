/*program test_program is 
    procedure print_string(integer level in)
        string s;
    begin    /* /* */ */   */
        a = a / 3
        putString("Enter a string:");
        getString(s); //test asdf awefawgt / awg aw/ 
        if (level < 3) then
            print_string(level + 1);
        end if;
        putString(s);
    end procedure;
begin
    print_string(0);
end program.
