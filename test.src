program test_program is
    integer fibb_result;
    string test;
    procedure fibb(integer n in, integer result out)
        integer a;
        integer b;
    begin

        result := n+1;
    end procedure;
begin
    fibb_result := 1;
    fibb(1, fibb_result);
    putInteger(fibb_result);
end program.
