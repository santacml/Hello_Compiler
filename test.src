/* ok so it's also time to test
	/* nested comments; can you do it??
	*/*/

PROGRAM grant IS
	global integer JAKE;
	global integer ryan[-3];
	global integer zach;
    integer test;
    
	procedure if_proc()
		begin
            zach:= 5;
	end procedure;
	begin
		if_proc();
		for_proc();
end program.
